{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "$raw = [Console]::In.ReadToEnd(); try { $d = $raw | ConvertFrom-Json; if ($d.tool_input.command -match 'git.*commit') { git -C 'c:\\Users\\jaume\\OneDrive\\documentos\\ERP-ACR-Estruch' push 2>$null } } catch {}",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
